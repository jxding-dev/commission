# ころんり 공지 사이트 — Claude Code Opus 구현 명세

## 0. 이 문서의 사용법

이 문서는 현재 폴더에 있는 이미지 5장을 이용해 실제 사이트를 처음부터 완성하기 위한 실행 명세다.

Claude Code는 아래 순서대로 작업하고, 각 단계가 끝날 때마다 관련 검증을 수행한다. 사용자가 따로 승인해야 할 위험한 작업이 아닌 이상 중간에 질문만 남기고 멈추지 말고, 합리적인 기본값을 적용하여 구현·검증까지 완료한다.

중요 원칙:

- 이 문서가 구현의 단일 기준이다.
- 현재 폴더의 기존 사용자 파일을 삭제하거나 덮어쓰지 않는다.
- 제공된 일러스트는 사이트 안에서만 사용하며 이미지 생성·변형을 하지 않는다.
- 작가의 결제·환불·저작권 정책을 임의로 확정하지 않는다.
- 확인되지 않은 정책은 명확한 `내용 준비 중` 상태로 표시한다.
- 화면을 화려하게 꾸미기보다 작품과 공지의 가독성을 우선한다.
- 구현 후 `npm run build`가 성공해야 한다.
- 데스크톱과 모바일을 실제 렌더링하여 가로 넘침, 이미지 잘림, 겹침을 점검한다.

---

## 1. 프로젝트 목표

활동명 `ころんり`의 개인 공지 사이트를 제작한다.

사이트는 다음 두 개의 실제 HTML 페이지로 구성한다.

1. `index.html` — 커미션 공지 및 주의사항
2. `adoption.html` — 캐릭터 분양 공지 및 주의사항

별도의 세 번째 홈 페이지를 만들지 않는다. `index.html`의 첫 화면이 사이트 소개와 메인 랜딩 역할을 함께 담당한다.

사이트가 전달해야 할 인상:

- 독립 일러스트레이터의 개인 아카이브
- 소규모 패션 룩북 같은 에디토리얼 구성
- 넓은 여백과 정돈된 타이포그래피
- 흰색·검정·회색 중심
- 절제된 장미색 포인트
- 캐릭터 그림이 가장 먼저 보이는 구조
- 귀엽지만 유치하지 않고, 약간의 고딕한 분위기

피해야 할 것:

- 반짝이, 파티클, 별이 흐르는 배경
- 네온과 과한 그라데이션
- 유리 질감 카드 남용
- 강한 그림자와 입체 버튼
- 과도한 둥근 모서리
- 자동 재생 음악
- 커스텀 마우스 커서
- 복잡한 스크롤 애니메이션
- 긴 텍스트를 이미지 위에 겹치는 구성

---

## 2. 기술 스택

기존 프로젝트가 없는 빈 폴더이므로 다음 구성으로 초기화한다.

- Vite
- 순수 HTML
- 순수 CSS
- 최소한의 Vanilla JavaScript
- ES Modules

React, Vue, Svelte, Tailwind, UI 컴포넌트 라이브러리는 사용하지 않는다. 이 사이트는 정적인 2페이지 안내 사이트이므로 복잡한 프레임워크가 필요하지 않다.

필요 파일의 권장 구조:

```text
/
├─ index.html
├─ adoption.html
├─ package.json
├─ vite.config.js
├─ src/
│  ├─ styles.css
│  └─ main.js
├─ public/
│  └─ images/
│     ├─ illustration-01.jpg
│     ├─ illustration-07.jpg
│     ├─ illustration-12.jpg
│     ├─ illustration-13.jpg
│     └─ illustration-14.jpg
└─ README.md
```

현재 루트의 원본 이미지:

```text
일러스트.jpg
일러스트7.jpg
일러스트12.jpg
일러스트13.jpg
일러스트14.jpg
```

원본은 그대로 보존한다. `public/images`에 웹에서 다루기 쉬운 영문 파일명으로 복사하여 사용한다.

이미지 대응:

| 원본 | 웹용 파일명 | 주요 용도 |
|---|---|---|
| `일러스트.jpg` | `illustration-01.jpg` | 캐릭터 분양 페이지 대표 이미지 |
| `일러스트7.jpg` | `illustration-07.jpg` | 분양 페이지 보조 이미지 |
| `일러스트12.jpg` | `illustration-12.jpg` | 커미션 페이지 가로 작품 이미지 |
| `일러스트13.jpg` | `illustration-13.jpg` | 커미션 메인 랜딩 대표 이미지 |
| `일러스트14.jpg` | `illustration-14.jpg` | 커미션 페이지 세로 작품 이미지 |

`package.json`에는 최소한 아래 스크립트가 있어야 한다.

```json
{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  }
}
```

Vite 다중 페이지 빌드에서 `index.html`과 `adoption.html`이 모두 결과물에 포함되는지 확인한다. 필요한 경우 `vite.config.js`의 Rollup input을 명시한다.

---

## 3. 콘텐츠 원칙

확정된 정보:

- 활동명: `ころんり`
- X: `https://x.com/88xoxox88`
- YouTube: `https://www.youtube.com/@qXoX-999p`
- 주 문의 수단: X
- 사이트 내부 결제 기능 없음
- 관리자 기능 없음

아직 확정되지 않은 정보:

- 커미션 타입과 가격
- 접수 상태
- 작업 기간
- 수정 횟수
- 결제 및 환불 기준
- 상업 이용 범위
- 캐릭터 분양 및 재분양 규정
- 신청 폼 주소

확정되지 않은 항목에 구체적인 횟수, 기간, 금액, 법적 조건을 창작하지 않는다.

대신 다음과 같이 표현한다.

- `상세 내용 준비 중`
- `신청 전 X를 통해 문의해 주세요`
- `커미션 타입과 가격은 샘플 준비 후 추가될 예정입니다`
- `분양 규정은 공개 전 최종 정리될 예정입니다`

사이트가 비어 보이지 않도록 실제 공지 구조와 항목명은 완성하되, 정책 값만 준비 중 상태로 둔다.

본문은 자연스러운 한국어로 작성한다. 제목에는 영문을 보조적으로 사용할 수 있다.

---

## 4. 공통 디자인 시스템

### 4.1 색상

CSS custom properties로 관리한다.

```css
--color-bg: #f7f6f3;
--color-surface: #ffffff;
--color-text: #171717;
--color-muted: #74716f;
--color-line: #ddd9d3;
--color-accent: #c94d58;
--color-accent-dark: #9f3946;
--color-mint: #82c9c5;
```

규칙:

- 화면의 약 80%는 아이보리·흰색·검정·회색으로 구성한다.
- 장미색은 링크, 현재 페이지, 번호, 상태 라벨에 제한적으로 사용한다.
- 민트는 아주 작은 보조 강조에만 사용한다.
- 본문 텍스트 대비는 WCAG AA 수준을 목표로 한다.

### 4.2 타이포그래피

외부 웹폰트 요청 실패로 레이아웃이 무너지지 않도록 견고한 폰트 스택을 사용한다.

권장:

```css
font-family:
  "Noto Sans KR",
  "Noto Sans JP",
  "Pretendard",
  "Apple SD Gothic Neo",
  "Malgun Gothic",
  system-ui,
  sans-serif;
```

폰트 파일을 직접 내려받아 포함하지 않는다. 외부 폰트를 사용한다면 반드시 system fallback을 유지한다.

대략적인 크기 범위:

- 대형 랜딩 제목: `clamp(3rem, 8vw, 7.5rem)`
- 페이지 제목: `clamp(2.2rem, 5vw, 5rem)`
- 섹션 제목: `clamp(1.4rem, 2.5vw, 2.25rem)`
- 본문: `clamp(0.98rem, 1.2vw, 1.08rem)`
- 본문 행간: 1.75 전후

### 4.3 형태

- 최대 콘텐츠 폭: 1280px
- 긴 본문 폭: 720px 전후
- 데스크톱 좌우 여백: 32~64px
- 모바일 좌우 여백: 20~24px
- 구분선: 1px
- 버튼 모서리: 0~4px 정도
- 카드 모서리: 최대 8px
- 강한 drop shadow 사용 금지

### 4.4 동작

허용:

- 링크 hover 시 밑줄 이동 또는 색상 변화
- 페이지 진입 시 150~300ms 정도의 약한 opacity/translate
- 이미지 hover 시 1.01 정도의 미세한 확대

필수:

- `prefers-reduced-motion: reduce` 대응
- 키보드 focus-visible 스타일
- 애니메이션이 없어도 모든 내용을 읽고 사용할 수 있어야 함

---

## 5. 공통 헤더와 푸터

### 5.1 헤더

데스크톱:

```text
ころんり              COMMISSION  ADOPTION  X  YOUTUBE
```

모바일에서도 페이지가 2개뿐이므로 햄버거 메뉴를 만들지 말고 메뉴를 직접 노출한다. 폭이 좁으면 상하 2줄로 자연스럽게 줄바꿈한다.

요구사항:

- 왼쪽 로고 `ころんり` 클릭 시 `index.html`
- COMMISSION → `index.html`
- ADOPTION → `adoption.html`
- X와 YouTube는 새 탭으로 열기
- 외부 링크에는 적절한 `rel="noopener noreferrer"`
- 현재 페이지 메뉴에 `aria-current="page"`
- 헤더는 sticky로 만들 수 있으나 모바일 화면을 과도하게 가리지 않게 한다.

### 5.2 푸터

```text
ころんり
Commission & Character Design

X  YouTube

© 현재 연도 ころんり
```

불필요한 개인정보나 이메일 주소를 생성하지 않는다.

---

## 6. `index.html` 상세 설계

### 6.1 메인 랜딩

PC에서 2열, 모바일에서 1열이다.

왼쪽:

```text
KORONRI
Commission &
Character Design

커미션과 캐릭터 분양에 관한
공지 및 이용 안내입니다.

[COMMISSION GUIDE]
[CHARACTER ADOPTION]
```

오른쪽:

- `illustration-13.jpg`
- 얼굴과 상반신을 중심으로 표시
- `object-fit: cover`
- 데스크톱 `object-position`과 모바일 `object-position`을 따로 조정
- 이미지 위에 본문을 겹치지 않음

### 6.2 페이지 소개

```text
01 / OVERVIEW
커미션 안내

신청 전 아래 안내사항을 확인해 주세요.
현재 상세 타입과 가격은 준비 중이며, 문의는 X를 통해 받고 있습니다.
```

짧은 정보 카드:

- 접수 상태 — `PREPARING`
- 문의 — `X DM`
- 타입 및 가격 — `준비 중`
- 결제 — `신청 확정 후 별도 안내`

숫자나 조건을 임의로 만들지 않는다.

### 6.3 페이지 내 목차

앵커 링크:

- 기본 안내
- 신청 방법
- 작업 과정
- 결제·취소
- 저작권·사용 범위
- 문의

sticky 목차가 본문을 가리키지 않게 하며, 모바일에서는 일반 가로 스크롤 링크 목록 또는 자연스러운 줄바꿈 목록으로 표시한다.

### 6.4 기본 안내

현재는 다음 문장으로 구성한다.

- 작업 가능한 범위와 어려운 소재는 정리 후 안내될 예정입니다.
- 타입과 가격은 샘플이 준비된 뒤 별도 항목으로 추가됩니다.
- 신청 전 X DM으로 일정과 작업 가능 여부를 문의해 주세요.

### 6.5 신청 방법

번호가 있는 세로 단계:

1. 공지 및 주의사항 확인
2. 신청 내용과 캐릭터 자료 준비
3. X DM으로 문의
4. 일정과 견적 확인
5. 신청 확정 후 작업 진행
6. 완성본 전달

신청 양식 예시를 복사 가능한 `<pre>`가 아니라 접근성과 반응형이 좋은 카드 또는 정의 목록으로 표시한다.

양식:

- 신청자명 / 입금자명
- 연락 가능한 계정
- 신청 내용 또는 희망 타입
- 캐릭터 자료
- 원하는 구도와 분위기
- 사용 목적
- 희망 일정
- 기타 요청사항

`양식 복사` 버튼을 제공해도 된다. 제공할 경우 Clipboard API 실패 시 텍스트 선택 또는 안내 문구로 안전하게 fallback한다. JavaScript 오류가 나도 페이지 이용에는 영향이 없어야 한다.

### 6.6 작업 과정

현재 확정되지 않은 수정 횟수나 기간을 쓰지 않는다.

```text
신청 및 상담 → 견적·일정 확인 → 작업 진행 → 확인 → 완성본 전달
```

하단에:

`세부 컨펌 단계와 수정 가능 범위는 타입 공개 시 함께 안내됩니다.`

### 6.7 결제·취소·환불

임의 정책 대신 다음 준비 중 문구를 사용한다.

```text
결제 방식과 취소·환불 기준은 정리 중입니다.
신청이 열리기 전 최종 규정을 이 페이지에 안내하며,
현재는 개인 신청이나 결제를 받고 있지 않습니다.
```

사이트 내부 카드·간편결제가 제공되는 것처럼 보이는 UI를 만들지 않는다.

### 6.8 저작권과 사용 범위

항목 구조만 보여준다.

- 개인적 사용
- SNS 및 프로필 사용
- 상업적 이용
- 수정 및 재배포
- 출처 표기
- 작업물 공개
- 생성형 AI 및 NFT 관련 규정

각 값은 `상세 기준 준비 중`으로 표시한다. 단, 확정되지 않은 금지 규정을 작가의 공식 정책처럼 단정하지 않는다.

### 6.9 작품 영역

`Selected Works`

- 넓은 가로 이미지: `illustration-12.jpg`
- 세로 보조 이미지: `illustration-14.jpg`
- 데스크톱에서는 비대칭 2열 에디토리얼 배치
- 모바일에서는 이미지가 한 열로 쌓임
- 원본의 중요한 얼굴과 전신이 잘리지 않도록 `object-fit: contain` 또는 작품별 object-position 사용
- 이미지 캡션은 `Selected work 01`, `Selected work 02`처럼 중립적으로 작성
- 가격이나 커미션 타입을 추측하지 않음

### 6.10 최종 문의 CTA

```text
안내사항을 확인하셨나요?
상세 문의는 X DM으로 부탁드립니다.

[X에서 문의하기]
```

---

## 7. `adoption.html` 상세 설계

### 7.1 대표 비주얼

- `illustration-01.jpg`
- PC에서 이미지와 제목을 비대칭 2열로 구성
- 원본의 검정 사선과 인물 구도를 훼손하지 않음
- 모바일에서는 화면 폭에 맞춰 전체 구도가 최대한 보이게 함

텍스트:

```text
CHARACTER ADOPTION
캐릭터 분양 및 이용 안내

캐릭터 디자인의 구매와 사용 전
아래 안내를 확인해 주세요.
```

### 7.2 현재 상태

```text
CURRENT STATUS
PREPARING

현재 공개된 분양 캐릭터가 없습니다.
새로운 분양 소식은 X를 통해 먼저 안내됩니다.

[X에서 소식 확인하기]
```

가짜 캐릭터 카드, 가격, SOLD 목록을 생성하지 않는다.

### 7.3 분양 절차

1. 캐릭터 및 규정 확인
2. X를 통한 구매 문의
3. 구매 가능 여부 확인
4. 입금 및 확인
5. 원본 파일 전달
6. 인수 완료

이는 향후 확정 전의 안내 구조이므로 `세부 절차는 분양 공개 시 최종 안내됩니다`를 함께 표시한다.

### 7.4 제공 파일

다음 항목을 나열하고 모두 `확정 전`으로 표시한다.

- 이미지 파일 형식
- 투명 배경 파일
- 설정 자료
- 원본 해상도
- 편집 파일 제공 여부
- 재전송 가능 기간

### 7.5 사용 가능 범위

항목:

- 개인 캐릭터 사용
- SNS 및 커뮤니티 활동
- 설정·이름·의상 수정
- 커미션 신청 자료
- 방송 및 영상
- 굿즈 및 상업적 이용

정책 값은 `상세 기준 준비 중`.

### 7.6 금지사항

항목 구조:

- 구매 전 이미지 사용
- 원작자 사칭
- 원본·수정본 재배포
- 디자인 복제
- 생성형 AI 관련 이용
- NFT 및 유사 용도

아직 공식 정책으로 확정되지 않았음을 명확히 표시한다.

### 7.7 수정·양도·재분양

항목:

- 디자인 변경 범위
- 무료 양도
- 재분양
- 재분양 가격
- 추가 커미션 비용 반영
- 원작자 통보
- 공동 소유

모두 `정책 준비 중`.

### 7.8 저작권과 출처

항목:

- 디자인 저작권
- 구매자가 얻는 이용 범위
- 원작자 표기
- 상업적 이용
- 포트폴리오 공개

임의로 권리 귀속을 확정하지 않는다.

### 7.9 보조 작품

- `illustration-07.jpg` 사용
- 페이지 중후반에 세로 이미지로 배치
- SNS 공유 썸네일 또는 OG 대표 이미지로는 사용하지 않는다.
- 이미지가 본문 가독성을 방해하지 않게 독립된 작품 영역으로 둔다.

### 7.10 최종 문의 CTA

```text
분양 및 이용 범위에 관한 문의는
X DM으로 부탁드립니다.

[X에서 문의하기]
```

---

## 8. 반응형 요구사항

필수 점검 폭:

- 320px
- 375px
- 430px
- 768px
- 1024px
- 1440px
- 1920px

조건:

- 모든 폭에서 가로 스크롤이 없어야 한다.
- `100vw` 때문에 스크롤바 폭만큼 넘치지 않게 한다.
- 이미지, 긴 URL, 영문 제목이 컨테이너 밖으로 넘치지 않아야 한다.
- 본문 줄 길이가 지나치게 길어지지 않아야 한다.
- 모바일에서 버튼이 너무 작거나 붙어 있지 않아야 한다.
- 헤더 메뉴가 로고와 겹치지 않아야 한다.
- 320px에서 메뉴가 자연스럽게 줄바꿈되어야 한다.
- 이미지의 핵심 얼굴과 전신이 무의미하게 잘리지 않아야 한다.
- 세로 이미지가 모바일 화면을 지나치게 길게 점유하지 않도록 균형을 잡되 원본 왜곡은 금지한다.
- `img`에는 `width`, `height`, `alt`, 적절한 `loading` 속성을 제공해 layout shift를 줄인다.
- 메인 대표 이미지는 lazy loading하지 않는다. 필요하면 `fetchpriority="high"`를 사용한다.

권장 breakpoint:

- 기본: 모바일 우선
- 640px 전후
- 900px 전후
- 1200px 전후

breakpoint 숫자보다 콘텐츠가 자연스럽게 배치되는지를 우선한다.

---

## 9. 접근성

필수:

- 올바른 문서 언어 `lang="ko"`
- 의미 있는 heading 순서
- `header`, `nav`, `main`, `section`, `footer` 사용
- 페이지마다 하나의 핵심 `h1`
- 장식 목적이 아닌 이미지에는 구체적인 한국어 alt
- 키보드만으로 모든 링크와 버튼 사용 가능
- `:focus-visible` 명확히 표시
- skip link 제공
- 색상만으로 상태를 전달하지 않음
- 외부 링크의 새 창 동작을 접근성 텍스트나 `aria-label`로 명확히 함
- prefers-reduced-motion 대응
- 터치 대상은 가급적 44px 이상

장문의 약관을 accordion 안에 전부 숨기지 않는다. 핵심 공지는 페이지에서 바로 읽을 수 있어야 한다.

---

## 10. SEO와 공유 정보

각 페이지에 서로 다른 title과 description을 설정한다.

권장:

`index.html`

```text
title: ころんり — 커미션 안내
description: ころんり의 커미션 공지, 신청 방법 및 주의사항을 안내합니다.
```

`adoption.html`

```text
title: ころんり — 캐릭터 분양 안내
description: ころんり의 캐릭터 분양 절차와 이용 주의사항을 안내합니다.
```

Open Graph:

- 커미션 페이지: `illustration-13.jpg`
- 분양 페이지: `illustration-01.jpg`

로컬 정적 사이트의 실제 배포 도메인이 없으므로 canonical URL이나 절대 OG URL을 임의의 가짜 도메인으로 만들지 않는다. 배포 도메인 확정 후 수정할 위치를 README에 기록한다.

---

## 11. 성능과 안정성

- 이미지 원본은 보존한다.
- 브라우저에서 불필요하게 거대한 픽셀을 로드하지 않도록 가능하면 웹용 파생 이미지를 생성한다.
- 품질이 눈에 띄게 떨어지지 않는 범위에서 크기 최적화한다.
- 파생 이미지 생성 도구를 추가할 경우 빌드 필수 의존성으로 과도하게 묶지 않는다.
- JavaScript 없이도 내비게이션과 모든 공지 내용을 이용할 수 있어야 한다.
- JavaScript는 선택적 기능에만 사용한다.
- 콘솔 오류와 404가 없어야 한다.
- HTML에서 존재하지 않는 이미지나 파일을 참조하지 않는다.
- 모든 내부 링크는 Vite dev와 build preview 양쪽에서 작동해야 한다.
- 정적 배포의 하위 경로에서도 깨질 수 있는 절대 `/` 경로를 남용하지 않는다. Vite의 base와 상대 경로를 고려한다.

---

## 12. 구현 순서

다음 순서를 지킨다.

### Step 1. 사전 점검

- 현재 폴더 파일 목록 확인
- 이미지 5장 존재 여부 확인
- 이미지 크기와 가로·세로 비율 확인
- 기존 파일이 있다면 먼저 읽고 보존

### Step 2. 프로젝트 초기화

- Vite용 `package.json` 생성
- 필요한 최소 파일 생성
- 이미지 원본을 보존하며 `public/images`에 영문명 복사
- 의존성 설치

### Step 3. 공통 기반

- 공통 CSS 변수와 reset
- 헤더, 푸터, 버튼, 섹션, 상태 라벨
- skip link와 focus 스타일
- 공통 JS의 안전한 초기화

### Step 4. 커미션 페이지

- 메인 랜딩
- 목차
- 공지 섹션
- 신청 절차
- 작품 영역
- 문의 CTA

### Step 5. 분양 페이지

- 대표 비주얼
- 준비 중 상태
- 분양 절차와 규정 항목
- 보조 작품
- 문의 CTA

### Step 6. 반응형 조정

- 모바일 우선 CSS 확인
- 320~1920px 점검
- 이미지별 object-position 조정
- 헤더, 버튼, 긴 제목, 링크 넘침 수정

### Step 7. 정적 검증

- HTML의 잘못된 중첩 확인
- 모든 이미지 경로 확인
- 모든 내부·외부 링크 확인
- 각 페이지의 h1, title, description 확인
- alt, width, height, loading 확인

### Step 8. 빌드 검증

- `npm run build`
- 빌드 오류 수정
- 결과물에 두 HTML 페이지가 모두 있는지 확인

### Step 9. 실제 렌더링 검증

- dev server 또는 preview server 실행
- 최소 375×812, 768×1024, 1440×900에서 두 페이지 확인
- 가능하면 Playwright로 스크린샷 생성
- 브라우저 콘솔 오류 확인
- 가로 overflow 탐지
- 깨진 이미지 확인
- 메뉴 이동과 외부 링크 href 확인

### Step 10. 마무리

- 발견된 문제 수정 후 다시 build
- README에 실행법, 페이지 구조, 이미지 매핑, 추후 수정 위치 기록
- 작업 결과와 검증 내역을 짧게 보고

---

## 13. 검증 체크리스트

완료 전에 아래를 모두 확인한다.

### 기능

- [ ] `index.html`이 정상적으로 열린다.
- [ ] `adoption.html`이 정상적으로 열린다.
- [ ] 두 페이지 사이 메뉴 이동이 작동한다.
- [ ] X 링크가 정확하다.
- [ ] YouTube 링크가 정확하다.
- [ ] 현재 페이지 메뉴가 구분된다.
- [ ] 선택 기능용 JavaScript 오류가 전체 페이지를 깨뜨리지 않는다.

### 콘텐츠

- [ ] 활동명이 모두 `ころんり`로 표기된다.
- [ ] 존재하지 않는 가격, 기간, 환불 조건을 만들지 않았다.
- [ ] 커미션 미정 항목은 준비 중으로 명확히 표시된다.
- [ ] 분양 중인 가짜 캐릭터를 만들지 않았다.
- [ ] 사이트 내 결제 기능이 있는 것처럼 표현하지 않았다.

### 이미지

- [ ] 이미지 5장이 계획된 위치에 올바르게 연결된다.
- [ ] 원본 이미지가 삭제·변형되지 않았다.
- [ ] 모든 이미지가 왜곡되지 않는다.
- [ ] 모바일에서 얼굴과 주요 구도가 무의미하게 잘리지 않는다.
- [ ] 이미지 alt가 작품의 핵심만 중립적으로 설명한다.
- [ ] 메인 이미지는 초기 화면에서 늦게 깜빡이지 않는다.

### 반응형

- [ ] 320px에서 가로 스크롤이 없다.
- [ ] 375px에서 헤더와 버튼이 겹치지 않는다.
- [ ] 430px에서 메뉴가 자연스럽다.
- [ ] 768px에서 태블릿 레이아웃이 어색하지 않다.
- [ ] 1024px 이상에서 본문이 지나치게 늘어나지 않는다.
- [ ] 1440px와 1920px에서 여백과 이미지 비율이 안정적이다.

### 접근성

- [ ] skip link가 작동한다.
- [ ] 키보드 포커스가 보인다.
- [ ] heading 순서가 올바르다.
- [ ] 색 대비가 충분하다.
- [ ] reduced motion 설정을 존중한다.
- [ ] 모든 터치 대상이 충분히 크다.

### 빌드

- [ ] `npm install`이 성공한다.
- [ ] `npm run build`가 성공한다.
- [ ] 빌드 결과에 `index.html`이 있다.
- [ ] 빌드 결과에 `adoption.html`이 있다.
- [ ] 콘솔 오류가 없다.
- [ ] 깨진 이미지와 404 요청이 없다.

---

## 14. 완료 조건

다음을 모두 충족해야 작업 완료로 간주한다.

1. 두 페이지가 실제로 구현되어 있다.
2. 제공된 이미지가 의도한 위치에서 정상적으로 보인다.
3. 모바일·태블릿·데스크톱에서 레이아웃이 안정적이다.
4. 확정되지 않은 운영 정책을 임의로 작성하지 않았다.
5. 모든 메뉴와 외부 링크가 작동한다.
6. 접근성 기본 요구사항이 적용되어 있다.
7. `npm run build`가 오류 없이 성공한다.
8. 실제 렌더링 점검 후 overflow와 콘솔 오류를 수정했다.
9. README에 실행 및 수정 방법이 정리되어 있다.

---

## 15. Claude Code Opus에게 줄 첫 프롬프트

아래 프롬프트를 Claude Code의 첫 메시지로 그대로 전달한다.

```text
현재 작업 폴더에 있는 `OPUS_IMPLEMENTATION_BRIEF.md`를 처음부터 끝까지 빠짐없이 읽고, 그 문서를 이 프로젝트의 단일 구현 기준으로 삼아 사이트를 완성해 주세요.

현재 폴더는 이미지 5장과 구현 명세만 있는 상태일 수 있습니다. 먼저 파일 목록과 이미지 크기·비율을 확인하고, 기존 사용자 파일과 원본 이미지는 삭제하거나 덮어쓰지 마세요.

그다음 명세에 정해진 순서대로 Vite 기반 순수 HTML/CSS/JavaScript 2페이지 사이트를 초기화하고 구현하세요. `index.html`은 커미션 공지 및 메인 랜딩, `adoption.html`은 캐릭터 분양 공지입니다. React, Vue, Tailwind 같은 불필요한 프레임워크는 추가하지 마세요.

중요:
- 확인되지 않은 가격, 기간, 수정 횟수, 결제·환불·저작권·재분양 조건을 임의로 창작하지 마세요.
- 미정 항목은 명세의 준비 중 표현을 사용하세요.
- 제공된 이미지 5장은 명세의 매핑과 용도에 맞춰 사용하고 원본을 보존하세요.
- 모바일 우선 반응형으로 작성하고 320, 375, 430, 768, 1024, 1440, 1920px 폭에서 가로 overflow와 겹침이 없도록 점검하세요.
- 접근성, 키보드 포커스, reduced motion, 이미지 alt와 크기 속성, SEO 메타 정보를 구현하세요.
- 작업 중 오류가 생기면 원인을 찾아 수정하고 계속 진행하세요. 위험한 삭제나 사용자 선택이 반드시 필요한 상황이 아니라면 질문만 남기고 멈추지 마세요.

구현 후 반드시 다음을 수행하세요.
1. `npm run build`
2. 빌드 결과에 두 HTML 페이지가 모두 포함되는지 확인
3. 로컬 서버로 두 페이지를 실제 렌더링
4. 모바일·태블릿·데스크톱 화면 점검
5. 브라우저 콘솔 오류, 깨진 이미지, 404, 가로 overflow 확인 및 수정
6. 수정 후 다시 `npm run build`
7. README에 실행법, 파일 구조, 이미지 매핑, 추후 공지문 수정 위치 작성

마지막 응답에는 구현한 내용, 생성·수정한 주요 파일, 실행한 검증, 남아 있는 미정 콘텐츠만 간결하게 보고하세요. 계획만 제시하지 말고 지금 바로 구현과 검증까지 완료하세요.
```
